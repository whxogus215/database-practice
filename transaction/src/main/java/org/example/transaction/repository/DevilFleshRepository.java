package org.example.transaction.repository;

import org.example.transaction.entity.DevilFlesh;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DevilFleshRepository extends JpaRepository<DevilFlesh, Long> {
}
